import { ExcludeIDsPipe } from './exclude-ids.pipe';

describe('ExcludeIDsPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcludeIDsPipe();
    expect(pipe).toBeTruthy();
  });
});
